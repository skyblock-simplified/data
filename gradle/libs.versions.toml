[versions]
simplified-annotations = "2.6.0"
junit-jupiter = "5.11.4"
hamcrest = "2.2"
hazelcast = "5.6.0"
spring-boot = "4.0.5"
micrometer = "1.14.5"

[libraries]
simplified-annotations = { group = "io.github.simplified-dev", name = "annotations", version.ref = "simplified-annotations" }
junit-jupiter-api = { group = "org.junit.jupiter", name = "junit-jupiter-api", version.ref = "junit-jupiter" }
junit-jupiter-engine = { group = "org.junit.jupiter", name = "junit-jupiter-engine", version.ref = "junit-jupiter" }
junit-platform-launcher = { group = "org.junit.platform", name = "junit-platform-launcher" }
hamcrest = { group = "org.hamcrest", name = "hamcrest", version.ref = "hamcrest" }
hazelcast = { group = "com.hazelcast", name = "hazelcast", version.ref = "hazelcast" }
spring-boot-starter-test = { group = "org.springframework.boot", name = "spring-boot-starter-webmvc-test", version.ref = "spring-boot" }
micrometer-registry-prometheus = { group = "io.micrometer", name = "micrometer-registry-prometheus", version.ref = "micrometer" }
